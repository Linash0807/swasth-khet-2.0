services:
  # Swasth Khet - Unified Web Service
  - type: web
    name: swasth-khet
    runtime: node
    buildCommand: npm run build:full
    startCommand: npm start
    rootDir: backend
    envVars:
      - key: NODE_ENV
        value: production
      - fromGroup: swasth-khet-common
